# 배포 작업 지침

사용자가 2026-09-14에 정정한 실제 사용 경로와 배포 완료 조건을 유지한다.

- 현재 이 PC의 사용자 실행 파일은 `C:/Users/user/Documents/GitHub/Nikke-Simul/artifacts/desktop/win-x64/Nikke Simul.exe`다. 바탕 화면 `Nikke Simul.lnk`도 같은 원본 경로를 사용한다. 사용자가 경로 변경을 명시하지 않는 한 임의로 worktree 실행본을 정식 사용 대상으로 대체하지 않는다.
- Director/다른 worktree의 구현·검수·빌드 완료와 원본 배포 완료를 구분한다. 원본 반영 요청에는 기존 변경을 보존하면서 원본 브랜치에 완료 커밋을 로컬 통합한다. 원격 push는 별도 요청이 있어야 한다.
- Git 통합만으로 Git 제외 EXE/DLL은 갱신되지 않는다. 반대로 EXE만 복사해도 UI는 갱신되지 않는다. 이 앱은 `desktop.settings.json`의 projectRoot에서 `apps/desktop-ui`를 제공하므로 코드/UI·백엔드·호스트·설정의 일치를 확인한다.
- 실행 파일까지 반영하는 작업에서는 대상 프로세스와 데이터 루트를 먼저 확인하고, 기존 배포 파일을 백업한 뒤 원본 경로를 빌드·갱신한다. 계정 DB·세션·캐시를 복제/초기화하거나 검증 때문에 원본 계정을 편집·동기화하지 않는다. 준비된 데이터는 `scripts/desktop.ps1 -Action build -DataRoot <기존 데이터 절대 경로> -Port 5180`로 재사용할 수 있다.
- 완료 보고 전 실제 사용자 실행 경로 또는 바로가기로 실행하여 프로세스 경로, API projectRoot, 최신 UI 리소스, 관련 화면, 계정·캐시 보존을 확인한다. 실행 중인 다른 서버를 임의 종료하지 않는다. Git 통합/빌드/배포/실행 검증 중 남은 단계가 있다면 전체 완료라고 보고하지 않는다.
- 보고서·README는 실제 사용 경로와 검증 범위를 명시한다. 이전 worktree 배포 문서가 남아 있으면 역사 기록임을 표시하고 현재 지침으로 연결한다.
