# P02 → P03 인계

P02 계산 API·UI는 구현되었고 히트 정수화 판정은 추후 실측 대조를 위해 열어 둔다. `HitCalculator.Compare`는 동일한 입력을 세 후보 정책으로 계산한다. 현재는 항상 `provisional_rounding`이다.

P03은 시간·발사·탄 소모·재장전·무기 특성부터 구현하고 트리거·조건·대상·스택·갱신·만료·대미지 effect 기록을 연결한다. 평타 계수의 원천 단위(/100)는 P02 경계를 재사용하고 OL normalizedValue를 다시 /10000하지 않는다. 차지식은 사용자 확정식을 유지한다.

P02의 기본 HitContext는 캐릭터 스킬·팀 버프 적용 전이다. 조건부 큐브와 애장품 스킬 교체, 명중·탄약·타이밍·크리 확률은 DeferredEffects에 기록되어 있다. 각 effect가 생성하는 타격에 공격력 기준·스냅샷 시점·크리/코어/차지 허용 여부를 명시해야 한다. 단일 타격 UI에서 유형을 바꾼 수동 검산 입력을 자동 스킬 구현으로 간주하지 않는다.

193명 수집과 160명 스탯 생성은 전투 효과 지원 범위가 아니다. 초기 후보 리타·블랑·누아르·앨리스·모더니아의 실제 effect 지원표를 만들고, SG 펠릿 계수/분할·MG ramp·차지/재장전 타이밍을 각각 검증한다. 실제 팀 게이지·버스트 사이클은 P04 목표다.

계산 자료는 P01 GameSnapshot과 별도의 immutable CalculationDataId로 기록한다. 향후 run은 계정 스냅샷·P01 매핑 ID·계산 자료 ID·규칙 버전·정수화 정책을 함께 고정해야 한다. 아직 히트 정수화가 확정되지 않은 결과를 추천 표본에 섞지 않는다.

기초 스탯의 C# 원본 대조 45,360개 수치 차이 0. 과거 실측 18점은 모든 후보에 일부 잔차가 남는다. 다음 관측은 UI 검산 기록과 함께 대조하며 임의 대미지 보정 계수를 추가하지 않는다. 현행 데이터팩/클라이언트 제한 조사로 정수화 순서는 확인하지 못했다.
