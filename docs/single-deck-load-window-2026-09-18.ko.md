# Q-LOAD-1000: 배터리 전원 단독 측정 구간

## 최신 결과: 1천회 pilot 수용 통과

QA 확정 `4c0c06ae0847c0464e35df08533aef1105092b21`의 `docs/single-deck-load-1000.ko.md`와 `artifacts/single-deck-qa/load1000-3db71912a601/final-audit.json`을 Director가 검토했다. final-audit에 기재된 근거29파일 SHA-256을 직접 대조해 불일치0, 제품40078d0 대비 src/apps/scripts/tools/data-pipeline 변경0을 확인했다. Director 재실행이 아니라 QA 실행 근거 검토에 따른 수용이다.

- 실제 측정10:09:46~10:11:02 KST, 배터리89→88%/Offline/SAMSUNG MODE 유지. 별도 calibration20 이후 pilot1000은61.975초, 전체 wall16.135회/초, worker2, 현 구간 측정캐시 재사용이다. 정확 compute-only 시간 또는 최적 병렬도 증거로 해석하지 않는다.
- 유효1000/1000, 실패/취소/중복/누락0, partial=false. 전체10페이지와 SQLite1020행(calibration20 포함) 대조, integrity_check 및 독립 통계 검산 통과. 튜닝5회는 정상저장 제외다.
- 팀 평균855537322.32, 평균95%CI[855240657.793,855833986.847]. 사전 컷855466067의 strict > 성공517/1000. 새 합성 계정/공개5인180초400고정DEF30925의 모델 결과이지 실제 사용자 장비 덱 또는 게임 관측 대조 결과가 아니다.
- 상태API p95 35.36ms, peak working set193785856B. 부하·보존·중단조건 근거 확인; 모든 외부 부하0이나 UI응답/온도/GC 수집 완료는 주장하지 않는다. 기능/저장/통계 pilot 수용 통과, 분포동등성·장시간 안정성·최적worker·AC/다른PC·추천은 미판정이다.

자기 API 종료 보고를 수신했으며 추가 측정은 배정하지 않았다. 최초 측정 예약은 이 완료로 조기 해제한다. 1만 단순예상10.33분/보수적18.49분,5만51.65분/80.47분은 선형 추정이다. 1만은 별도 약25분 구간과 평가 사전 기준을 확보한 뒤 진행하고, 5만은 별도 예약한다. 남은 최초 구간을 이유로 새 규모를 자동 실행하지 않는다. 제품 통합/원본 배포/GitHub 업로드는 이번에도 미수행이다. 아래 착수 지시·상태는 역사 기록이다.

## 사용자 승인과 범위

2026-09-18 사용자가 다음 단계 착수와 지금부터 약20~30분 PC 사용 가능을 확인했다. 전원 조회 Offline/배터리95%를 알린 뒤 사용자는 **배터리 상태로 측정**을 선택했다. 최초 구간은 **2026-09-18 10:02~10:30 KST**다. 준비도 이 구간을 소비한다. 시간 연장이나 1만/5만 실행은 승인된 것으로 간주하지 않는다. 이번 1천회는 지속부하 pilot이며 최종 독립 평가/추천 표본이 아니다.

10:01~10:02 Orca runtime `5dba61c3-6dac-47aa-86ae-224121c0a4f7` 조회: Backend/엔진/통계/UI는 inactive/liveTerminalCount0, 검수는 기존 Q-TUNE 완료 후 대기다. 현재 dotnet/msbuild/testhost/python 실행은 발견되지 않았으며 node3개는 존재한다. 이는 시점 관측이지 지속 독점 증명이 아니다. Director는 이 구간에 별도 빌드/테스트/시뮬레이션/배포를 실행하지 않고 비활성 담당을 깨우지 않는다. 다른 프로세스를 강제 종료하지 않는다.

장치 관측: i7-1360P, 물리12/논리16, CPU LoadPercentage2(단일 관측). 전원 구성표 `ab6534a3-bc02-4c44-94d1-a8535b2eb070` / SAMSUNG MODE, PowerLineStatus Offline, 배터리95%. 전원 모드/우선순위/드라이버/절전 설정은 바꾸지 않는다. 배터리 환경 결과를 AC/다른PC/전역 최적성으로 일반화하지 않는다.

## 기존 검수 담당 단독 실행 지시

소유: 기존 QA worktree `C:/Users/user/orca/workspaces/Nikke-Simul/검수`, 기존 Q-TUNE 담당 handle `term_234e279b-927e-4118-a8fe-f90736db2e68`. 같은 worktree의 별도 모델선택 터미널에는 작업을 보내지 않는다. 신규 worker/Run/Dispatch/lifecycle 생성 없음. 회신 Director는 현재 handle `term_6e774787-1510-4736-872a-8b9be666e2b0`다. 오래된 runtime의 handle을 재사용하지 않는다.

1. 자기 QA HEAD606b568과 제품40078d0/보고서d8be9d3을 확인하고 기존 변경/package-lock을 보존한다. 이 지시서와 자기 보고서의 사전 분포 gate/순차 부하 계획을 읽는다. 제품 코드 수정 금지; 필요한 실행기·검사·문서만 자기 QA 소유 경로에 추가한다.
2. 준비/Release 빌드는 측정 전 완료하고 로그를 분리한다. 원본 계정/세션 복제 없이 기존 허용 공개12파일 + 새 합성 계정으로 동일 리타/블랑/앨리스/누아르/모더니아,400,180초,고정DEF30925,저장 합성 택틱을 사용한다. 입력/스냅샷/data/engine/rules/build/tactic fingerprint를 고정·보존한다. 실제 사용자 장비 덱이나 게임 영점 검증이라고 하지 않는다.
3. 시작 시 본인·다른 Orca 작업 상태와 OS 부하/전원/배터리/메모리/디스크 여유를 재확인한다. 경쟁 benchmark나 새 빌드가 있으면 측정을 시작하지 않고 보고한다. 읽기 전용 관측만 허용한다. 자동 CPU cpu-policy-3 및 명시 MaxWorkers=2, 현행 메모리 보호를 사용하고 자원/선택/캐시 출처를 기록한다. 초기 측정은 새 격리 캐시에서 시작하여 과거 공유부하 캐시를 성능 근거로 쓰지 않는다.
4. 별도 calibration 실험으로 정상20회 완전전투를 측정하고 전체/준비/튜닝/정상배치 시간을 분리한다. 그 표본은 1천회에 섞지 않는다. 실제 처리량으로1천회 예상 종료 시간을 계산하고 오버헤드와 보수적 여유를 포함해 10:30 이전 완료가 가능할 때만 **새 실험 ID, phase=pilot, 요청1000회**를 시작한다. 불가능하면 calibration/예상시간만 보고한다. 입력/제품/분포 경계 결함이 발견되면 대량 시작 없이 보고한다.
5. 1천회는 유효 index 정확히0..999/중복·누락0, 시도별 실패·취소·정상0 구분, 팀=5인 합계, 전체 페이지 일관성, 독립 평균/SD/HF7/평균CI/컷 초과 및 Wilson을 검산한다. 컷은 평가 결과를 보기 전에 calibration 근거로 고정·기록한다. 팀/5인 통계와 실제 유효n을 남기며 warmup/calibration/취소 표본 제외를 검증한다. 1천회는 pilot이므로 기존 Hoeffding/DKW bounds·허용폭 preregistration 없이 분포동등성 통과나 자동추천을 주장하지 않는다.
6. 시작·진행·종료의 벽시계/프로세스CPU/working set·private bytes/가용메모리/배터리·전원과 선택workers, 저장소 크기·정상 run/s, API polling p95를 낮은 빈도로 기록한다. 다른 프로세스는 이름/PID/CPU 부하만 확인하고 불필요한 명령줄·민감정보는 수집하지 않는다. 할당량/GC/온도/화면응답 등 측정 도구가 없는 항목은 null+사유로 남긴다. API p95를 UI p95라고 하지 않는다. 새 profiler/대형 의존 설치 금지.
7. **10:30 도달, 배터리30% 이하, AC/전원 모드 변경, 절전·잠금에 따른 실행중단, 지속 경쟁부하, 메모리 보호 실패/결과 불일치** 시 본인 API로 안전 취소하고 종료를 확인하여 부분 결과와 사유를 보존한다. 전원 변경 전후를 같은 성능 결과로 합치지 않는다. 경쟁부하의 판단 기준은 실행 전에 정하고 기록한다. 10:30 후 긴 재실행을 시작하지 않는다.
8. 원본 계정DB/세션/캐시/EXE/5180/5181 및 타worktree/package-lock 보존, 공개 원본12개 hash 전후 확인. 자기 격리 서버만 정리. 1만/5만/GPU/추가OL/원본 병합·배포/원격 생성push는 이번에 하지 않는다.
9. 새 artifacts와 확정 QA 커밋/보고서, 시작·종료시각/전원조건、calibration과1000 실측/추정 구분、완료n/통계/보존/부하 지속 관측、미수용 범위 및1만/5만 예상시간을 Director에 한 번 인계 후 대기한다. 늦어도 창 종료 시 진행상태를 보고하고 준비 지연으로 실행하지 못했으면 미실행으로 명시한다. 실패·부분·미판정을 통과로 바꾸지 않는다.

## 상태

기존 검수 handle `term_234e279b-927e-4118-a8fe-f90736db2e68`에 한 번 전달했다. 요청 `2cb981c5-4a72-4ad4-ab48-f0e3acb652ac`는 accepted=true/input_accepted/turn_started, runtime `5dba61c3-6dac-47aa-86ae-224121c0a4f7`다. 준비 작업 착수이며 실제1천회 시작·완료는 아직 확인되지 않았다. 승인된 구간 종료 후 예약은 자동 해제되며 후속 구간을 임의 연장하지 않는다.
