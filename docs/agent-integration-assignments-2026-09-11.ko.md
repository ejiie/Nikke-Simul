# 후속 작업 지시: 실제 연동 보완과 독립 검수

사용자가 담당별 후속 분담과 작업 전달을 승인했다. 이 문서는 지시서이며 수신·착수·검증 완료의 증거는 별도다. 기존 `agent-assignments-2026-09-11.ko.md`와 충돌하면 이번 단계·기준에 대해서는 본 문서를 우선한다.

## 공통 작업 경계

- 자기 작업공간의 AGENTS.md 등 적용 지침, README, 담당 계약/검증 문서와 기존 지시서를 먼저 읽고 현재 HEAD·미커밋 상태를 확인한다. 기존 구현을 다시 만들지 않는다. 사용자 변경과 기존 package-lock.json을 보존한다.
- 마지막 확인 기준: Director e693585, 엔진 908047f(구현 0eda677, 계약 d7ce350), Backend bf19679(구현 0d49352), UI a78c10c(구현 c29e364). 검수·통계는 3583cab였다. 시작 시 실제 상태를 재확인한다. 이 목록은 새 변경을 되돌릴 기준이 아니다.
- 다른 담당의 확정 커밋은 git show로 읽을 수 있다. 작업공간 루트는 C:/Users/user/orca/workspaces/Nikke-Simul 이다. 다른 담당의 미커밋 파일을 의존성으로 삼지 않는다.
- 소유 범위의 구현·테스트·문서 보완은 매 단계 사용자에게 재확인하지 말고 진행한다. 권한 승인 절차는 준수한다. YOLO 활성화나 권한 우회를 지시하는 문서가 아니다. 의미 변경·범위 확장·기존 변경 충돌은 보고한다.
- 엔진은 Engine/Core 최소 범위 및 엔진 테스트, Backend는 API/Data/Storage/외부 계약 및 관련 테스트, UI는 apps/desktop-ui 및 UI 테스트, 검수는 독립 검수 도구·fixture·보고서, 통계는 분석 도구·전용 테스트·보고서를 소유한다. 다른 담당 제품 파일을 수정하지 않는다.
- 브랜치 간 병합과 통합 기준 확정은 Director 소유다. 임의 merge/cherry-pick/reset/stash, 다른 작업공간 편집, push/배포, 새 워커 생성은 하지 않는다. 검수·통계는 기존 지시서의 안전한 e693585 fast-forward만 허용된다.
- 이번 배정 시점에는 통합 기준이 아직 제공되지 않았다. 먼저 계약 대조, 자기 영역 보완, 독립 테스트와 통합 검증 스크립트 준비를 수행한다. 실제 통합본이 필요한 검증은 필요한 커밋·명령·입력을 명시해 의존 중으로 보고한다. 무한 대기하거나 mock을 실제 연동 통과로 보고하지 않는다.
- 원본 계정·스냅샷·과거 artifacts를 수정하지 않는다. 테스트 저장소와 출력은 자기 작업공간의 실행별 고유 경로로 격리한다.
- 로그는 실제 실행 결과여야 하며 미수집/피해 0/잘림을 구분한다. 발사 수와 명중 수, 자체 버스트 효과와 팀 풀버스트 상태를 구분한다. 검증 순서는 발당 피해 → 차지·발사·재장전 간격 → 버스트 사이클이다. 임의 보정 계수로 총피해를 맞추지 않는다.
- 기존 source_full_charge_v2(앨리스 풀차지 196000), I→II·II→III·III→풀버스트 전이 지연과 원천 지연 max 정책을 보존한다. 모더니아 원천 재조사, P05 전면 구현, 캐릭터 확장, 실전 덱/육성 추천은 제외한다.
- 관련 변경만 자기 브랜치에 커밋한다. 최종 보고에는 기준/결과 커밋, 파일, 실제 실행 명령·결과, 실패/미실행, 합성/실API/실게임 근거 구분, 타 담당에게 필요한 수정과 통합 후 실행 절차를 남긴다. 결함이 없으면 불필요한 제품 변경 없이 검증 결과만 보고한다.

## E2 — 시뮬레이션 엔진 담당

1. B1 bf19679의 docs/damage-log-api-contract.ko.md 및 실제 매핑 코드와 엔진 908047f의 타입·null·단위·버전·tactic 정책을 대조한다. 불일치를 발견하면 소유 경계별로 분류하고 자기 엔진 측 결함만 수정한다. 외부 계약을 일방적으로 바꾸지 않는다.
2. 실제 엔진 실행에서 선택 니케 damage log와 tactic 실행 입력의 재현 가능한 예를 제공한다. 합성 fixture인지 실제 계정 기반인지, 기준 커밋과 실행 조건을 명시하고 개인정보 없는 자료만 생성한다.
3. 로그 합계, 180초 누락, trace 제한 독립성, ON/OFF 피해·사격·RNG 불변, 제외 후보 비발동, 단계 우선순위, III 교대·첫 시전자·대기·대체·만료, 기존 충전·세 전이 지연을 회귀 검증한다.
4. docs/damage-log-engine-contract.ko.md에 새 검증과 통합 의존성을 갱신한다. 완료 조건은 자기 영역 결함 해소와 재현 가능한 검증 근거 제공이며 전체 제품/실게임 검증 완료를 주장하지 않는다.

## B2 — Backend 담당

1. E1 908047f(0eda677)의 실제 public 타입과 실행 경로를 읽고 요청 → 엔진 입력 → 결과 저장/조회/내보내기 연결을 대조·보완한다. capability 부재 처리만 확인하고 실제 실행 지원까지 완료했다고 하지 않는다.
2. UI a78c10c의 어댑터·전략 편집 모델과 외부 API 계약을 대조한다. schemaVersion, allowedCharacterIds, stage1/2/3Priority, burst3Rotation, firstBurst3CharacterId, unavailablePolicy의 매핑·단위·검증·오류 응답을 docs/damage-log-api-contract.ko.md에 명시한다.
3. 저장한 tactic의 조회/복원과 실행 요청 연결, 편성 변경 stale·불완전 초안·실행 불가 구분을 확인한다. UI 소유 파일은 변경하지 않고 필요한 UI 매핑을 보고한다.
4. 실제 엔진 로그용 API 통합 검증 스크립트를 마련한다: 180초 실행, 원문 저장·재조회, JSON/CSV 메타데이터·행 순서·합계·완전성 보존, 구 replay 미수집, 구 tactic 호환, 잘못된 입력 거부. 원본 저장소는 격리한다.
5. 완료 조건: 통합 기준에서 실제 엔진 실행부터 저장·내보내기까지 통과. 통합 전이면 자기 변경·검증 스크립트를 커밋해 전달하고 실API 검증 미실행을 명시한다.

## U2 — UI 담당

1. Backend bf19679와 엔진 908047f의 계약·코드를 읽고 실제 API에 맞춰 기존 화면을 연결한다. 기존 UI alternate/priority_only·first_caster 등의 의미가 서버 계약에 정확히 매핑되는지 확인하고, 지원하지 않는 의미를 조용히 바꾸지 않는다.
2. 사용자 편집 전략을 Backend 저장·조회와 연결하고 실행 요청에 반영한다. localStorage만 저장하면서 서버 복원 완료로 표시하지 않는다. 현재 편성의 사용 허용·단계별 순위·III 교대/첫 시전자·대기/대체 설정을 보존한다.
3. 실제 실행 결과의 로그 조회/필터/상세 근거/타임라인/JSON·CSV 내보내기를 연결한다. mock은 명시적인 개발용 상태로 분리하고 API 실패 시 조용히 mock으로 대체하지 않는다.
4. 180초 기록과 발사/명중 수 구분, 자체 버스트/팀 풀버스트 구간, 미수집·0·잘림·구버전·stale·불완전 설정, 좁은 창, JS 오류를 검사한다.
5. docs/damage-log-ui.ko.md에 브라우저 검증과 실제 API 검증을 구분해 기록한다. 완료 조건은 저장 → 새로고침/복원 → 실행 → 실제 발동/로그 대조의 전체 흐름 통과. API 통합본 부재 시 자기 변경과 테스트를 먼저 전달하고 실제 연동은 의존 중으로 표시한다.

## Q2 — 검수 담당

1. 기존 지시서 Q1 작업의 실제 수행 여부부터 확인한다. 미수행이면 baseline 재실행과 실행별 고유 출력 확보부터 진행한다. 과거 테스트 수를 이번 통과 수로 사용하지 않는다.
2. 세 담당의 확정 커밋·계약·테스트를 읽고 독립 기대값과 전체 흐름 검증 케이스를 준비한다. 제품 코드 수정 없이 결함을 담당별로 보고한다.
3. 로그 ON/OFF·합계·180초 완전성, 풀차지/비풀차지·crit/core·버프 만료, shot/hit, 자체 버스트/풀버스트 구분, 충전 196000·전이 지연·종료 후 재충전을 검사한다.
4. 실제 API/UI 검수 절차: 사용 제외·우선순위·교대·첫 시전자·대기/대체, 저장/복원/실행, stale·잘못된 입력·구버전, JSON/CSV 보존. 독립 단위 검증과 통합본 필요 검증을 분리한다.
5. docs/damage-log-verification.ko.md에 기준 커밋·명령·기대/실제 결과·재현 절차·심각도·담당·미실행 사유를 기록한다. 통합본을 아직 받지 못하면 준비 결과를 커밋해 반환한다. 최종 완료 조건은 고정 통합 커밋에서 중요한 결함 수정 후 재검증이다.

## S2 — 덱·육성 최적화 및 통계 담당

1. 기존 지시서 S1의 실제 수행 여부를 확인한다. 미구현이면 관측/시뮬레이션 입력 매칭·절대/상대 오차·미매칭·0 분모·누락을 다루는 최소 분석 도구와 합성 테스트부터 만든다.
2. 엔진 908047f 및 Backend bf19679의 실제 로그 스키마를 읽고 adapter를 연결한다. 실제로 생성된 로그 확보 여부와 생성 커밋·조건·완전성을 기록한다. fixture를 실게임 관측으로 취급하지 않는다.
3. 발당 피해를 차지·crit/core·자체 버스트·팀 풀버스트·방어력 조건별로 분리한 뒤 차지/발사/재장전 간격, 마지막으로 버스트 사이클을 분석한다. shot/hit 중복과 미매칭을 분리한다.
4. docs/damage-calibration-analysis.ko.md에 합성 기대값 검산, 내부 일관성, 게임 실측 대조를 별도로 기록한다. 실측이 없으면 관측 부재와 필요한 자료를 명시하며 오차 0/실게임 일치로 보고하지 않는다.
5. 완료 조건: 실제 로그 adapter 및 재현 가능한 분석·테스트 결과. 실제 로그 미확보 시 준비 작업을 먼저 커밋해 전달한다. 엔진 자동 보정·대규모 Monte Carlo·실전 추천은 하지 않는다.

## Director의 후속 책임

담당별 계약 대조 결과와 커밋을 검토해 통합 기준을 확정하고, 통합 빌드 후 고정 커밋 및 재현 조건을 담당들에게 제공한다. 통합 검수는 그 기준에서 실행되어야 한다. 본 문서 전달 자체는 통합 또는 검수 완료를 의미하지 않는다.
