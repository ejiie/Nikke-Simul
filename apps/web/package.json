{
  "name": "nikke-sync-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "vite --host 127.0.0.1 --port 5174 --strictPort", "build": "tsc --noEmit && vite build", "test": "vitest run --maxWorkers=2" },
  "devDependencies": { "typescript": "7.0.2", "vite": "8.2.1", "vitest": "4.1.10", "@types/node": "26.2.0" }
}
